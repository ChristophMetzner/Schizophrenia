import pandas as pd
import numpy as np

def Heischberger(tc):
    return C
def 

class random_net:
    """
    Contains methods to generate a random reference network that is matched to the input network,
    which can later be used for further graph analysis
    """
    def __init__(self, timecourse, method):
        """
        :param timecourse: n x p dimenisional array
        :param method: Rewire or Heischberger
        """
        if not isinstance(timecourse, (np.ndarray, pd.DataFrame):
            raise ValueError('Timecourse must be in array like format')
        self.timecourse=timecourse
        self.
    def
